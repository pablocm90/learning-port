<div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
  <div class="flex justify-between items-center mb-8">
    <h1 class="text-3xl font-bold text-text">Edit Learning Item</h1>

    <%= button_to "Delete", admin_learning_item_path(@learning_item), method: :delete, data: { turbo_confirm: "Are you sure you want to delete this learning item?" }, class: "bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 transition" %>
  </div>

  <%= render "form", learning_item: @learning_item %>
</div>
